{
  "name": "@admissible/relayer",
  "private": true,
  "version": "0.1.0",
  "description": "Server-side relayer: holds the funded Creditcoin key and submits browser-built Attestcoin proofs on the browser's behalf, so the /app proof theatre reaches stage 5 without shipping a private key to the client.",
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts"
  },
  "dependencies": {
    "@admissible/sdk": "workspace:*",
    "dotenv": "^17.4.2",
    "ethers": "^6.15.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}
