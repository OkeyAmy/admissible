{
  "name": "@admissible/bench",
  "private": true,
  "version": "0.1.0",
  "description": "Evidence generator — mirrors real EAS attestations into the Creditcoin registry at volume and writes receipts/mirrors.jsonl + receipts/summary.json.",
  "type": "module",
  "scripts": {
    "bench": "tsx src/index.ts",
    "summary": "tsx src/summarize.ts"
  },
  "dependencies": {
    "@admissible/sdk": "workspace:*",
    "dotenv": "^17.4.2",
    "ethers": "^6.15.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}
